# API Configuration
VITE_TENANT_API_BASE_URL=http://localhost:5099
VITE_AUTH_API_BASE_URL=http://localhost:5050
VITE_ADMIN_API_BASE_URL=http://localhost:5001
VITE_API_BASE_URL=http://localhost:5099

# Default Auth Token (optional)
# If set, this token will be automatically used for all API requests
# when no token is available in cookies or auth state.
# Useful for development, testing, and automated environments.
# Example: VITE_DEFAULT_AUTH_TOKEN=your_jwt_token_here
VITE_DEFAULT_AUTH_TOKEN=

# For Docker Compose usage, you can also set these variables:
# DEFAULT_AUTH_TOKEN=your_jwt_token_here

# Note: The token priority is:
# 1. Auth state (after successful login)
# 2. Cookie token
# 3. VITE_DEFAULT_AUTH_TOKEN (environment variable)

